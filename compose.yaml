services:
  trayce:
    build: .
    ports:
      - "9740:9740"
    volumes:
      - /tmp/trayce:/tmp/trayce
    environment:
      - TRAYCE_HOST=0.0.0.0
      - TRAYCE_PORT=9740
    restart: unless-stopped
